model: embeddinggemma-300m
label:
  en_US: "EmbeddingGemma 300M"
model_type: text-embedding
model_properties:
  # This is the approved FlowFlox model profile's conservative input limit.
  context_size: 2048
  # FlowFlox's own embedding client batches 24 chunks; use the same bound so
  # Dify's document indexing stays comfortably within the running pool.
  max_chunks: 24
pricing:
  input: "0"
  unit: "0.001"
  currency: USD
