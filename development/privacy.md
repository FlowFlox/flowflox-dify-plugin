# FlowFlox Dify Provider Privacy

The provider sends only the Dify prompt, selected automatic capability profile,
and user-configured generation parameters to the FlowFlox application URL.
Its credential is used only for FlowFlox authentication. The provider does not
send prompts or credentials to any other service and does not persist them.
